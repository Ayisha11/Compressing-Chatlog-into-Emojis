# Emoji Compress Project

**Short description:**  
A small Python project that compresses chat text by mapping short tokens and phrases to emoji seeds (and vice versa). Useful for compact messaging or preprocessing conversational logs.


## Features
- Convert common phrases / abbreviations to emoji seeds.
- Dynamic dictionary generation and compression.
- Preprocessing utilities for chat datasets.
- `app.py` demo script for quick testing.


